#include<stdio.h>
#include<stdlib.h>
int*ptr;
int*ptr2;
ptr=(int*)malloc(6*size of (int));
for(int i=o;i<6;i++){
ptr2=(int*)malloc(600000*size of (int));
printf("Enter the value of %d element is\n",i);
scanf("%d",&ptr[i]);
free(ptr2);
}
for(int i=0;i<6;i++){
printf("The value of %d element is %d\n",i,ptr[i]);
}
return(0);
}



























