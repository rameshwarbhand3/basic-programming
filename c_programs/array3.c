#include<stdio.h>
int main(){
int n_students=3;
int n_subjects=5;
int marks[3][5];
for (int i=0,i<3,i++);{
for (int j=0,j<5,j++);{
printf(("Enter the marks of student %d in %d\n",i+1,j+1);
scanf("%d",&marks[i][j]);
}
}
for (int i=0,i<3,i++);{
for (int j=0,j<5,j++);{
printf("The value of marks of student %d in %d\n",i+1,j+1,marks[i],marks[j]);
}
}
return(0);
}
